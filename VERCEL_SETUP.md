# Vercel Deployment Setup - VerifyAI

## 🚀 تفعيل النشر التلقائي على Vercel

### الخطوة 1: ربط Vercel بـ GitHub

1. اذهب إلى: https://vercel.com/new
2. اختر "Import Git Repository"
3. ابحث عن: `5n90/verifyai.vercel.app`
4. اختر المستودع

### الخطوة 2: إعدادات المشروع

**اسم المشروع:**
```
verifyai
```

**الإطار (Framework):**
```
Other
```

**جذر المشروع (Root Directory):**
```
./
```

### الخطوة 3: متغيرات البيئة

أضف المتغيرات التالية في "Environment Variables":

```
GEMINI_API_KEY = AIzaSyANg93fYEK5HuDWbyVXW7B1CeHHCL3bQaE
FLASK_ENV = production
FLASK_DEBUG = False
SECRET_KEY = your-secret-key-here
```

### الخطوة 4: الأوامر

**Build Command:**
```
pip install -r requirements.txt
```

**Output Directory:**
```
public
```

**Install Command:**
```
pip install -r requirements.txt
```

### الخطوة 5: النشر

اضغط "Deploy" وانتظر انتهاء البناء (عادة 2-3 دقائق).

---

## ✅ التحقق من النشر

بعد النشر الناجح:

1. ستحصل على رابط مثل: `https://verifyai.vercel.app`
2. اختبر الـ API:
```bash
curl https://verifyai.vercel.app/api/health
```

3. يجب أن ترى:
```json
{
  "status": "healthy",
  "timestamp": "2026-02-20T..."
}
```

---

## 🔄 النشر التلقائي

بعد ربط Vercel بـ GitHub:

- ✅ كل `push` إلى `main` = نشر تلقائي
- ✅ كل `pull request` = preview URL
- ✅ كل `merge` = نشر للإنتاج

---

## 🛡️ إعدادات الأمان

### تفعيل HTTPS
- ✅ مفعل افتراضياً في Vercel
- ✅ شهادة SSL مجانية

### إضافة Domain مخصص (اختياري)
1. اذهب إلى "Settings" → "Domains"
2. أضف: `verifyai.com` أو اسم domain آخر
3. حدّث DNS records

### تفعيل Branch Protection
في GitHub:
1. اذهب إلى "Settings" → "Branches"
2. اختر "Add rule"
3. اختر `main`
4. فعّل "Require pull request reviews"

---

## 📊 المراقبة والتنبيهات

### في Vercel Dashboard:
1. اذهب إلى "Analytics"
2. راقب:
   - Response Time
   - Error Rate
   - Deployment Status

### إشعارات Slack (اختياري)
1. اذهب إلى "Settings" → "Integrations"
2. ابحث عن "Slack"
3. أضف Slack Workspace

---

## 🔧 استكشاف الأخطاء

### المشكلة: فشل البناء
```
الحل:
1. تحقق من requirements.txt
2. تحقق من api/index.py
3. شاهد السجلات في Vercel Dashboard
```

### المشكلة: خطأ في متغيرات البيئة
```
الحل:
1. تحقق من GEMINI_API_KEY
2. تأكد من أنه صحيح وفعال
3. أعد النشر بعد التحديث
```

### المشكلة: timeout في API
```
الحل:
1. زيادة maxDuration في vercel.json
2. تحسين أداء الكود
3. استخدام caching
```

---

## 📈 الأداء المتوقع

| المقياس | القيمة |
|--------|--------|
| Build Time | 1-2 دقيقة |
| Response Time | 15-25ms |
| Uptime | 99.95% |
| Cold Start | < 500ms |

---

## 🎯 الخطوات التالية

- [ ] ربط Vercel بـ GitHub
- [ ] إضافة متغيرات البيئة
- [ ] بدء النشر الأول
- [ ] اختبار الـ API
- [ ] إضافة domain مخصص
- [ ] تفعيل المراقبة

---

**تم الإنشاء:** 2026-02-20  
**الحالة:** ✅ جاهز للنشر
